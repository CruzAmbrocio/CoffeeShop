/**
 * @format
 */
import { registerRootComponent } from 'expo';
import App from './App';
import { AppRegistry } from 'react-native';

// registerRootComponent(App);
AppRegistry.registerComponent('CoffeeShop', () => App);
